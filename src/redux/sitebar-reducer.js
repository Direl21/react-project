let initialState = {
    onlineFriends: [
        { id: 1, name: 'Kata' },
        { id: 2, name: 'Bard' },
        { id: 3, name: 'Lulu' }
    ]
};

const sitebarReducer = (state = initialState, action) => {


    return state;
}

export default sitebarReducer;